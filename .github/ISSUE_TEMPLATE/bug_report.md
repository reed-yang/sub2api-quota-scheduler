---
name: Bug report
about: Something the scheduler did (or did not do) that contradicts the README
labels: bug
---

**sub2api version**: 
**sub2api-quota-scheduler version** (`sub2api-quota-scheduler version`): 
**Mode**: shadow / apply

**What happened**

**What you expected**

**Decision log line** (from `decisions.jsonl`; account names are fine, no keys):

```json
```

**Journal** (`journalctl -u sub2api-quota-scheduler -n 20 -o cat`, redacted):

```
```
