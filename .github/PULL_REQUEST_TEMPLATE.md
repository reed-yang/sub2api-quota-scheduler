## What changes

## Why

## Checklist

- [ ] `gofmt -l . && go vet ./... && go test ./...` is clean
- [ ] Tests cover the behavior change
- [ ] No new fields are written to sub2api (or the README's safety section is updated)
- [ ] Tested against sub2api version: 
